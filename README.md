# Framework - Кастомный JavaScript фреймворк

Легковесный JavaScript фреймворк для создания веб-приложений с реактивной системой управления состоянием и компонентной архитектурой.

## 📋 Описание проекта

Этот проект представляет собой учебный фреймворк, демонстрирующий основные концепции современных frontend-фреймворков (таких как React, Vue) в упрощенном виде. Фреймворк включает систему управления состоянием, компонентную модель и автоматическое обновление UI при изменении данных.

В качестве демонстрации реализовано приложение для управления контактами с возможностью просмотра, создания и управления списком контактов через REST API.

## 🚀 Основные возможности фреймворка

### 1. Система управления состоянием (State Management)

#### Глобальное состояние

- **`initState(initialState, renderFn?)`** - инициализация глобального состояния приложения
- **`getState()`** - получение текущего состояния
- **`setState(newState)`** - обновление состояния с автоматической перерисовкой компонентов

```javascript
// Инициализация
initState({
  contacts: [],
  loading: false,
  error: null,
});

// Получение состояния
const state = getState();
console.log(state.contacts);

// Обновление состояния
setState({ contacts: [...contacts, newContact] });
// Все подписанные компоненты автоматически обновятся!
```

#### Локальное состояние компонентов

- **`useState(initialValue, componentId?)`** - хук для создания локального состояния (аналог React.useState)

```javascript
function Counter() {
  const [count, setCount] = useState(0, "counter-id");

  return createElement(
    "div",
    {},
    createElement("p", {}, "Счет: ", count),
    createElement(
      "button",
      {
        onClick: () => setCount(count + 1),
      },
      "Увеличить"
    )
  );
}
```

### 2. Создание DOM элементов

**`createElement(tag, attributes, ...children)`** - функция для декларативного создания DOM элементов

```javascript
createElement(
  "div",
  {
    className: "card",
    style: { color: "red" },
    onClick: () => console.log("clicked"),
  },
  createElement("h1", {}, "Заголовок"),
  createElement("p", {}, "Текст")
);
```

**Особенности:**

- Поддержка `className` для CSS классов
- Объектный синтаксис для `style`
- Автоматическая обработка событий (onClick, onSubmit и т.д.)
- Поддержка вложенных элементов и массивов
- Обработка примитивных значений (строки, числа)

### 3. Компонентная система

#### Создание компонентов

Компоненты - это обычные функции, которые возвращают DOM элементы:

```javascript
function ContactsList() {
  const state = getState();

  return createElement(
    "div",
    {},
    ...state.contacts.map((contact) => ContactCard(contact))
  );
}
```

#### Монтирование компонентов

**`mount(component, container)`** - монтирует компонент в DOM и настраивает автоматическое обновление

```javascript
// Монтирование в контейнер
mount(ContactsList, "#contactsContainer");

// При изменении state через setState()
// компонент автоматически перерисуется!
```

**Особенности:**

- Автоматическая подписка на изменения глобального состояния
- Эффективное обновление DOM (замена только измененных элементов)
- Поддержка селекторов и DOM элементов в качестве контейнера

### 4. Автоматическое обновление UI

Фреймворк реализует реактивную систему обновлений:

- При вызове `setState()` все подписанные компоненты автоматически перерисовываются
- Обновление происходит только для замонтированных компонентов
- Минимальные изменения DOM (замена только измененных узлов)

## 🎓 Простой фреймворк для новичков

Для тех, кто только начинает изучать фреймворки, доступна упрощенная версия без системы состояния - `simpleFrameworks.js`. Это минималистичная реализация с всего двумя функциями, которая поможет понять основы работы с компонентами.

### Основные функции

#### 1. `createElement(tag, attributes, ...children)`

Создает HTML элемент декларативным способом.

```javascript
import { createElement } from "./simpleFrameworks.js";

const button = createElement(
  "button",
  {
    className: "my-button",
    onClick: () => alert("Привет!"),
  },
  "Нажми меня"
);

const card = createElement(
  "div",
  { className: "card" },
  createElement("h1", {}, "Заголовок"),
  createElement("p", {}, "Текст карточки"),
  button
);
```

**Поддерживаемые атрибуты:**

- `className` - для CSS классов
- `style` - объект со стилями
- `onClick`, `onSubmit`, `onInput` и другие обработчики событий
- Любые другие HTML атрибуты (`id`, `type`, `placeholder` и т.д.)

#### 2. `render(component, containerId)`

Рендерит компонент в контейнер по ID. Очищает контейнер и добавляет туда компонент.

```javascript
import { createElement, render } from "./simpleFrameworks.js";

// Создаем компонент (просто функция, которая возвращает элемент)
function MyComponent() {
  return createElement(
    "div",
    { className: "my-component" },
    createElement("h1", {}, "Привет, мир!"),
    createElement(
      "button",
      {
        onClick: () => alert("Кнопка нажата!"),
      },
      "Нажми меня"
    )
  );
}

// В HTML должен быть элемент с id="app"
// <div id="app"></div>

// Рендерим компонент
render(MyComponent, "app");
```

### Пример простого приложения

```javascript
import { createElement, render } from "./simpleFrameworks.js";

// Компонент списка задач
function TodoList() {
  const todos = ["Купить молоко", "Позвонить маме", "Сделать домашку"];

  return createElement(
    "div",
    { className: "todo-list" },
    createElement("h2", {}, "Мои задачи"),
    createElement(
      "ul",
      {},
      ...todos.map((todo) =>
        createElement("li", { className: "todo-item" }, todo)
      )
    )
  );
}

// Компонент формы
function ContactForm() {
  return createElement(
    "form",
    {
      onSubmit: (e) => {
        e.preventDefault();
        alert("Форма отправлена!");
      },
    },
    createElement("input", {
      type: "text",
      placeholder: "Имя",
      required: true,
    }),
    createElement("input", {
      type: "email",
      placeholder: "Email",
      required: true,
    }),
    createElement("button", { type: "submit" }, "Отправить")
  );
}

// Рендерим компоненты
render(TodoList, "todos");
render(ContactForm, "form");
```

**HTML:**

```html
<div id="todos"></div>
<div id="form"></div>
```

### Отличия от полной версии

| Простая версия          | Полная версия                                 |
| ----------------------- | --------------------------------------------- |
| Нет системы состояния   | Есть глобальное и локальное состояние         |
| Ручной вызов `render()` | Автоматическое обновление при изменении state |
| 2 функции               | 7+ функций                                    |
| Идеально для обучения   | Для реальных приложений                       |

### Когда использовать простую версию

- ✅ Изучение основ компонентного подхода
- ✅ Простые статические страницы
- ✅ Понимание работы с DOM
- ✅ Первые шаги в программировании

### Когда использовать полную версию

- ✅ Приложения с динамическими данными
- ✅ Нужна реактивность (автообновление UI)
- ✅ Работа с API и состоянием
- ✅ Более сложная логика приложения

## 📁 Структура проекта

```
framework/
├── src/
│   ├── frameworks.js         # Ядро фреймворка (state, компоненты, createElement)
│   ├── simpleFrameworks.js  # Упрощенная версия для новичков (без state)
│   ├── api.js                # API модуль для работы с REST API
│   ├── index.js              # Главный файл приложения (демо)
│   ├── index.html            # HTML шаблон
│   └── styles.css            # Стили приложения
├── webpack.config.js         # Конфигурация Webpack
├── package.json              # Зависимости и скрипты
└── README.md                 # Документация
```

## 🛠 Установка и запуск

### Требования

- Node.js (версия 14+)
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Режим разработки

Запускает dev-сервер с hot-reload:

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:8080` (или другой порт, если 8080 занят)

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 💡 Примеры использования

### Базовый компонент с глобальным состоянием

```javascript
import {
  createElement,
  initState,
  setState,
  getState,
  mount,
} from "./frameworks.js";

// Инициализация состояния
initState({ count: 0 });

// Компонент счетчика
function Counter() {
  const state = getState();

  return createElement(
    "div",
    {},
    createElement("p", {}, `Счетчик: ${state.count}`),
    createElement(
      "button",
      {
        onClick: () => setState({ count: state.count + 1 }),
      },
      "Увеличить"
    )
  );
}

// Монтирование
mount(Counter, "#app");
```

### Компонент с локальным состоянием

```javascript
import { createElement, useState, mount } from "./frameworks.js";

function TodoList() {
  const [todos, setTodos] = useState([], "todo-list");
  const [input, setInput] = useState("", "todo-input");

  const addTodo = () => {
    if (input.trim()) {
      setTodos([...todos, input]);
      setInput("");
    }
  };

  return createElement(
    "div",
    {},
    createElement("input", {
      value: input,
      onInput: (e) => setInput(e.target.value),
      placeholder: "Новая задача",
    }),
    createElement("button", { onClick: addTodo }, "Добавить"),
    createElement(
      "ul",
      {},
      ...todos.map((todo) => createElement("li", {}, todo))
    )
  );
}

mount(TodoList, "#app");
```

### Работа с API

```javascript
import { getContacts, createContact } from "./api.js";
import { setState, getState } from "./frameworks.js";

// Загрузка данных
async function loadContacts() {
  setState({ loading: true });

  try {
    const contacts = await getContacts();
    setState({ contacts, loading: false });
  } catch (error) {
    setState({ error: error.message, loading: false });
  }
}

// Создание контакта
async function addContact(contactData) {
  const newContact = await createContact(contactData);
  const state = getState();
  setState({ contacts: [...state.contacts, newContact] });
}
```

## 🔧 API модуль

Модуль `api.js` предоставляет функции для работы с REST API:

- **`getContacts()`** - получить все контакты
- **`getContactById(id)`** - получить контакт по ID
- **`createContact(contactData)`** - создать новый контакт
- **`updateContact(id, contactData)`** - обновить контакт
- **`deleteContact(id)`** - удалить контакт

Все функции возвращают Promise и обрабатывают ошибки.

## 🎨 Особенности реализации

### Реактивность

- Используется паттерн Observer для подписки компонентов на изменения
- При изменении state автоматически вызываются функции обновления всех подписанных компонентов

### Управление состоянием

- Глобальное состояние хранится в одном объекте
- Локальные состояния компонентов хранятся в Map с уникальными ID
- Поддержка функциональных обновлений для useState (как в React)

### Рендеринг

- Компоненты рендерятся в DOM элементы при монтировании
- При обновлении создается новый элемент и заменяется старый
- Сохраняется позиция элемента в DOM дереве

### События

- Автоматическое преобразование атрибутов вида `onClick`, `onSubmit` в обработчики событий
- Поддержка всех стандартных DOM событий

## 📚 Технологии

- **JavaScript (ES6+)** - основной язык
- **Webpack** - сборщик модулей
- **Babel** - транспиляция ES6+ в ES5
- **CSS** - стилизация

## 🎯 Демо-приложение

В проекте реализовано демо-приложение "Управление контактами", которое демонстрирует:

- Загрузку списка контактов с сервера
- Отображение контактов в виде карточек
- Создание новых контактов через форму
- Обработку состояний загрузки и ошибок
- Категоризацию контактов (Друг, Коллега, Семья и т.д.)
- Флаги избранного и VIP статуса

## 🔍 Отличия от популярных фреймворков

Этот фреймворк является упрощенной версией и не включает:

- Виртуальный DOM (используется прямое манипулирование DOM)
- Диффинг алгоритмы (полная замена элементов при обновлении)
- Жизненный цикл компонентов (componentDidMount, useEffect и т.д.)
- Контекст и провайдеры
- Роутинг
- Оптимизации производительности (memo, useMemo и т.д.)

Однако он демонстрирует основные концепции:

- Компонентный подход
- Реактивное обновление UI
- Управление состоянием
- Декларативный синтаксис

## 📝 Лицензия

ISC

## 👤 Автор

Проект создан в образовательных целях для изучения принципов работы современных frontend-фреймворков.
